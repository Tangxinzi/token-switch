# Downloads

下载文件不直接提交到 Git 仓库。

正式发布时，请把以下文件上传到 GitHub Releases 的对应版本：

- `Token Switch_<version>_universal.dmg`
- `Token Switch_<version>_universal.tar.gz`
- `Token Switch_<version>_universal.tar.gz.sig`

上传后再更新仓库根目录的 `update.json`。
